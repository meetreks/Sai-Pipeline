pipeline {
    agent any
    stages {
        stage('Pull Git Trigger') {
            steps {
                git poll: true, url: 'https://github.com/meetreks/Java-DevOps.git'
            }
        }
        stage('Maven-Clean') {
            steps {
                bat "mvn -Dmaven.test.failure.ignore=true clean package"
            }
        }


    }
}